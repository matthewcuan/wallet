# Server
PORT=3000
HOST=0.0.0.0

# Toggle the mock signer. Set to false once a real Pass Type ID cert is wired up.
MOCK_SIGNER=true

# Pass identifiers (from Apple Developer portal; required when MOCK_SIGNER=false)
PASS_TYPE_IDENTIFIER=pass.com.example.wallet
TEAM_IDENTIFIER=
ORGANIZATION_NAME=Wallet

# Certificates (paths relative to backend/, both required when MOCK_SIGNER=false)
PASS_CERT_PATH=./certs/pass.p12
PASS_CERT_PASSWORD=
WWDR_CERT_PATH=./certs/wwdr.pem
