# Server
PORT=3000
HOST=0.0.0.0

# Toggle the mock signer. Set to false once a real Pass Type ID cert is wired up.
MOCK_SIGNER=true

# Pass identifiers (from Apple Developer portal; required when MOCK_SIGNER=false)
PASS_TYPE_IDENTIFIER=pass.com.example.wallet
TEAM_IDENTIFIER=
ORGANIZATION_NAME=Wallet

# Certificates (PEM format; required when MOCK_SIGNER=false).
# To produce these from a .p12 exported from the Apple Developer portal:
#   openssl pkcs12 -in pass.p12 -clcerts -nokeys -out signerCert.pem
#   openssl pkcs12 -in pass.p12 -nocerts        -out signerKey.pem
# Place the WWDR intermediate cert (downloaded from Apple) at WWDR_CERT_PATH.
SIGNER_CERT_PATH=./certs/signerCert.pem
SIGNER_KEY_PATH=./certs/signerKey.pem
SIGNER_KEY_PASSPHRASE=
WWDR_CERT_PATH=./certs/wwdr.pem

# Pass templates directory. Must contain one subdirectory per pass type
# (storeCard, generic, coupon, eventTicket, boardingPass) with the
# Apple-required assets (pass.json, icon.png/@2x/@3x, logo.png/@2x/@3x).
TEMPLATES_PATH=./templates
